services:
  fastapi:
    build: ./backend
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    env_file:
      - .env
  frontend:
    build: ./frontend
    command: npm run dev
    ports:
      - "3000:3000"


